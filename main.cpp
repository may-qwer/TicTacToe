#include <iostream>
#include "./classes/game.h"
#include "./classes/queue.h"

using namespace std;

int main() {
    Game game;
    game.main_cyrcle();

    // Queue q(3);
    // q.push(1, 2);
    // q.push(2, 3);
    // q.push(3, 4);
    // q.push(4, 5);

    // int x, y;
    // q.peek(2, x, y);

    return 0;
}